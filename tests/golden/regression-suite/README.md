# TLC Parity Regression Suite (Placeholder)

This directory will host the golden parity specifications used by the
`tlc-parity` harness. Populate it with the PlusCal/TLA+ models that
should run during `scripts/dev/check-all.sh`.

Until real specifications land, the directory remains empty so the
parity harness can run in stub mode without failing.
